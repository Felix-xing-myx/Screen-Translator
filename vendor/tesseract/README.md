Place the redistributable Windows Tesseract runtime in this directory before
building. It should include `tesseract.exe`, required DLL files, and the
English language data file at `tessdata/eng.traineddata`.

